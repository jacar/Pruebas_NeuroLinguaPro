{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],

  "builds": [
    {
      "src": "server.py",
      "use": "@vercel/python",
      "env": {
        "TTS_HOME": ".tts_models"
      }
    }
  ],
  "buildCommand": "pip install -r requirements.txt"
}