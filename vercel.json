{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],

  "builds": [
    {
      "src": "server.py",
      "use": "@vercel/python"
    }
  ],
  "buildCommand": "pip install -r requirements.txt",
  "env": {
    "TTS_HOME": ".tts_models"
  }
}