{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "pip install -r requirements.txt",
  "builds": [
    {
      "src": "server.py",
      "use": "@vercel/python"
    }
  ]
}